language: node_js
node_js:
  - "6"
services:
  - mysql
before_script:
  - echo "DROP DATABASE scavenger; CREATE DATABASE scavenger; USE scavenger; USE mysql;\nUPDATE user SET password=PASSWORD('root') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root